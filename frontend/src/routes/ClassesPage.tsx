// Placeholder for Frontend Spec §4.3/4.5: join/create a class, class
// updates feed. Wire against endpoints.ts's createClass/joinClass/
// listAnnouncements/postAnnouncement once the real screens exist.
export function ClassesPage() {
  return (
    <div style={{ padding: "1.5rem" }}>
      <h2 style={{ fontSize: "var(--font-size-h2)" }}>Classes</h2>
      <p style={{ color: "var(--color-text-secondary)" }}>
        TODO: join/create class flow, class list, announcements feed (see
        lib/api/endpoints.ts for the available calls, Frontend Spec §4.3
        and §4.5 for the intended UX).
      </p>
    </div>
  );
}
